# ===================================================================
# Spring Cloud Consul Config bootstrap configuration for the "prod" profile
# ===================================================================
spring:
    cloud:
        consul:
            config:
                fail-fast: true
                format: yaml
                profile-separator: '-'
            retry:
                initial-interval: 1000
                max-interval: 2000
                max-attempts: 100
